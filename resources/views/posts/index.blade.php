{{-- <x-app-layout>
    <div class="card p-10">
        <div class="border-b-2">
            @foreach ($posts as $post)
                <div class="flex items-center overflow-hidden bg-black md:w-10/12 mx-auto">
                    <img src="{{ asset('storage/' . $post->image) }}" alt="{{ $post->description }}"
                        class="object-cover w-full">
                </div>
            @endforeach
        </div>
    </div>
</x-app-layout> --}}